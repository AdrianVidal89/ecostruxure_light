# Settings package.
#
# Select the active settings module via the DJANGO_SETTINGS_MODULE environment
# variable. Defaults are wired in manage.py / wsgi.py / asgi.py to
# ``config.settings.development``. Use ``config.settings.production`` in prod.
