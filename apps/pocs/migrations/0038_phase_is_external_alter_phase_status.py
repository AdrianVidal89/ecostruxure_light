# Generated by Django 5.2.15 on 2026-07-28 15:48

from django.db import migrations, models


def migrate_external_status_to_flag(apps, schema_editor):
    """Improvement brief item 1: External stops being a ``status`` value and
    becomes an independent flag. Phases previously frozen at status='external'
    get ``is_external=True`` and a reasonable initial progress status
    ('pending' — there's no real per-phase progress data to derive a better
    one from, since External phases never had their own tasks/tests)."""
    Phase = apps.get_model('pocs', 'Phase')
    Phase.objects.filter(status='external').update(
        is_external=True, status='pending'
    )


class Migration(migrations.Migration):

    dependencies = [
        ('pocs', '0037_team_phase_external_at_phase_external_by_and_more'),
    ]

    operations = [
        migrations.AddField(
            model_name='phase',
            name='is_external',
            field=models.BooleanField(default=False),
        ),
        migrations.RunPython(
            migrate_external_status_to_flag, migrations.RunPython.noop
        ),
        migrations.AlterField(
            model_name='phase',
            name='status',
            field=models.CharField(choices=[('pending', 'Pending'), ('in_progress', 'In progress'), ('completed', 'Completed'), ('not_applicable', 'Not applicable')], default='pending', max_length=20),
        ),
    ]
